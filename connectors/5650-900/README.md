# Connector 5650 / 900

## Purpose

This folder defines the governance-only connector specification pack for institutional sources identified as `5650` and `900`.

## Status

This connector is documentation only.

The identifiers `5650` and `900` are treated as declared source identifiers pending institutional confirmation.

Their technical origin must not be assumed at this stage.

## Governance

This connector is governed by:
- `sad-moron-framework`
- declared source protocol
- temporal boundary protocol
- evidence classification protocol
- connector compliance protocol

## Connector Preconditions

This connector requires:
1. source owner identification
2. source access method confirmation
3. institutional purpose
4. temporal reconstruction window
5. allowed fields
6. excluded fields
7. audit/evidence expectations
8. known data limitations

## Implementation Block

This connector cannot move to implementation until `5650` and `900` are formally described in the declared sources catalog.

## Prohibitions

This folder does not authorize:
- extraction
- runtime code
- API code
- credential storage
- scraping
- source inference by convenience

## Summary

This folder establishes a governed boundary for identifiers `5650` and `900` before any implementation exists.
